
function(input, output, session) {
  router_server()
  observe({
  
    #---------------------------------------------------------------------------
    # Other Analysis
    # source("server_scripts/other_analysis_server.r", local = T)
    
  })
}
